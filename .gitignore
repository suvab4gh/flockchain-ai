# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Production bundle
build/

# Environment files — NEVER commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Stellar session keys — contain private keys, never commit
scratch/session_key.json

# Python ML virtual environment and cache
ml/.venv/
ml/__pycache__/
ml/logs/
__pycache__/
*.pyc
*.pyo

# ML model binaries (large — retrain locally)
ml/models/*.pkl
ml/models/*.pt
ml/models/*.zip

# Vercel
.vercel

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db
